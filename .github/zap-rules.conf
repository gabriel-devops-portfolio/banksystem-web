# ZAP rules configuration file
# Use 'IGNORE' to skip a rule, or 'INFO' to report but not fail.

10015   IGNORE  (Incomplete or No Cache-control Header)
10020   IGNORE  (X-Frame-Options Header Not Set)
10021   IGNORE  (X-Content-Type-Options Header Missing)
10038   INFO    (Content Security Policy (CSP) Header Not Set)# ZAP rules configuration file
# Use 'IGNORE' to skip a rule, or 'INFO' to report but not fail.

10015   IGNORE  (Incomplete or No Cache-control Header)
10020   IGNORE  (X-Frame-Options Header Not Set)
10021   IGNORE  (X-Content-Type-Options Header Missing)
10038   INFO    (Content Security Policy (CSP) Header Not Set)
